<form method="post" action="{{ path('for_borrow_delete', {'id': for_borrow.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ for_borrow.id) }}">
    <button class="btn">Delete</button>
</form>
