{% extends 'base.html.twig' %}

{% block title %}NewsletterAdd Admin{% endblock %}

    {% block body %}
        <div class="ht__breadcrumb__area bg-image--6" style="background-image: url('{{asset('assets/images/bg/6.jpg')}}')">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="breadcrumb__inner text-center">
                            <h2 class="breadcrumb-title">Newsletter</h2>
                            <nav class="breadcrumb-content">
                                <a class="breadcrumb_item" href="{{ path('home') }}">Newsletter </a>
                                <span class="brd-separator">/</span>
                                <span class="breadcrumb_item active">Add</span>
                            </nav>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <br><br>
        <div class="container">
                <h1>Create New Newsletter</h1>
                {{ form(form) }}
            </div>

            <br>
    {% endblock %}
